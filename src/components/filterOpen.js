import React from 'react';
import connect from 'react-redux';

import * as _filter from '../redux/actions/filterActions';
import '../stylesheets/Filter.scss';
const FilterOpen = (props) => {
  return(<>

  </>)
}

export default connect(null)(FilterOpen);